# Build results
[Dd]ebug/
[Rr]elease/
[Bb]in/
[Oo]bj/

# Visual Studio
.vs/
*.user

# Claude Code
.claude/*
!.claude/settings.json
!.claude/scripts/

# Superpowers brainstorming visual companion
.superpowers/
.claude/spikes/

# Scratch output (screenshots from screenshot-editor.ps1, throwaway artifacts)
scratch/

# Coverage artifacts (coverlet.msbuild output + `dotnet test --collect` results)
[Tt]est[Rr]esults/
coverage.cobertura*.xml
.maintenance.json

# aislop runtime artifacts (session/history logs, caches, baselines).
# Unanchored so nested project directories are covered too.
**/.aislop/*
!**/.aislop/config.yml
