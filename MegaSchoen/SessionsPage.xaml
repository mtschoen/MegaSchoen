<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MegaSchoen.ViewModels"
             xmlns:controls="clr-namespace:MegaSchoen.Controls"
             x:Class="MegaSchoen.SessionsPage"
             Title="Claude Sessions">

    <Grid>
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Label Grid.Column="0"
                       Text="Active Claude sessions"
                       FontSize="22"
                       FontAttributes="Bold"
                       VerticalOptions="Center"/>
                <Button Grid.Column="1"
                        Text="Refresh"
                        Command="{Binding RefreshCommand}"
                        HeightRequest="36"
                        WidthRequest="100"
                        VerticalOptions="Center"/>
            </Grid>

            <VerticalStackLayout BindableLayout.ItemsSource="{Binding HostStatuses}" Spacing="2">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="vm:HostStatusViewModel">
                        <HorizontalStackLayout Spacing="6">
                            <Label Text="●" TextColor="{Binding StatusColor}" FontSize="12" VerticalOptions="Center"/>
                            <Label Text="{Binding Host}" FontSize="12" FontAttributes="Bold" VerticalOptions="Center"/>
                            <Label Text="{Binding StatusText}" FontSize="12" TextColor="{StaticResource Gray500}" VerticalOptions="Center"/>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>

            <Label Text="No active Claude sessions"
                   IsVisible="{Binding Sessions.Count, Converter={StaticResource IsZeroConverter}}"
                   TextColor="{StaticResource Gray500}"
                   HorizontalOptions="Center"
                   Margin="20"/>

            <CollectionView ItemsSource="{Binding Sessions}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:SessionCardViewModel">
                        <Frame Margin="0,4" Padding="12" BorderColor="{StaticResource Gray300}">
                            <VerticalStackLayout Spacing="6">
                                <Grid ColumnDefinitions="80,*,Auto,Auto" ColumnSpacing="10">
                                    <Label Grid.Column="0"
                                           Text="{Binding StateText}"
                                           TextColor="{Binding StateColor}"
                                           FontAttributes="Bold"
                                           VerticalOptions="Center"/>
                                    <Label Grid.Column="1"
                                           Text="{Binding CwdShort}"
                                           VerticalOptions="Center"
                                           FontSize="13"/>
                                    <Label Grid.Column="2"
                                           Text="{Binding LastActivityRelative}"
                                           TextColor="{StaticResource Gray500}"
                                           FontSize="11"
                                           VerticalOptions="Center"/>
                                    <Button Grid.Column="3"
                                            Text="Focus"
                                            IsVisible="{Binding FocusButtonVisible}"
                                            IsEnabled="{Binding CanFocus}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SessionsPageViewModel}}, Path=FocusCommand}"
                                            CommandParameter="{Binding .}"
                                            HeightRequest="36"
                                            WidthRequest="80"
                                            VerticalOptions="Center"/>
                                </Grid>
                                <HorizontalStackLayout Spacing="8">
                                    <Label Text="{Binding SessionIdStem}"
                                           FontSize="11"
                                           TextColor="{StaticResource Gray500}"/>
                                    <Label Text="{Binding HostLabel}"
                                           IsVisible="{Binding HostLabel, Converter={StaticResource IsNonEmptyStringConverter}}"
                                           FontSize="11"
                                           TextColor="{StaticResource Gray500}"/>
                                </HorizontalStackLayout>
                                <Label Text="{Binding SubagentSummary}"
                                       IsVisible="{Binding SubagentSummary, Converter={StaticResource IsNonEmptyStringConverter}}"
                                       FontSize="11"
                                       TextColor="{StaticResource Gray500}"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Frame BorderColor="{StaticResource Primary}"
                   BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}"
                   Padding="15">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Claude Cycler (debug)"
                           FontSize="20"
                           FontAttributes="Bold"/>
                    <Button x:Name="CyclePermsButton"
                            Text="Cycle Pending Permissions"
                            Clicked="OnCyclePermsClicked"
                            BackgroundColor="{StaticResource Primary}"/>
                    <Button x:Name="CycleAnyWaitingButton"
                            Text="Cycle Any Waiting"
                            Clicked="OnCycleAnyWaitingClicked"
                            BackgroundColor="{StaticResource Primary}"/>
                    <Label x:Name="CycleClaudeStatusLabel"
                           Text=""
                           FontSize="12"
                           TextColor="{StaticResource Gray600}"/>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
        <controls:VersionStampView />
    </Grid>

</ContentPage>
